# Not shipped in this repo at all -- granules, tile labels, training shards, feature
# caches. Too heavy to distribute (one sensor's U-Net shards alone were 24 GB). See
# README.md's "Building the training data from raw input swaths" for how to regenerate
# this directory locally; it's ignored here rather than partially tracked.
data/

__pycache__/
*.pyc
.DS_Store
.pytest_cache/
*.egg-info/
build/
